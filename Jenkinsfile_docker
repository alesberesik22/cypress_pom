node {
    checkout scm
    sh 'env'
    def version = '$BRANCH_NAME'
    sh 'echo test ${version}'

//     docker.withRegistry('https://registry.hub.docker.com', 'DOCKER') {

      //   def customImage = docker.build("ales/beresik:${env.BUILD_ID}")

        /* Push the container to the custom Registry */
      //   customImage.push()
//     }
}